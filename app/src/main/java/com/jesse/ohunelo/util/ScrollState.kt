package com.jesse.ohunelo.util

// This is used to represent scroll state of scrollable screens, so if the user scrolls up scroll
// state is STATE_SCROLL_UP and vice versa.
enum class ScrollState {
    STATE_SCROLL_UP, STATE_SCROLL_DOWN
}